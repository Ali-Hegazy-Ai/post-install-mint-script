#!/usr/bin/env bash

: "${APT_RETRIES:=3}"
: "${MAX_JOBS:=2}"
: "${NVM_VERSION:=v0.40.3}"
: "${MIN_LOW_RAM_MB:=4000}"
: "${BATCH_SIZE_DEFAULT:=15}"
: "${BATCH_SIZE_LOW_RAM:=5}"
: "${NO_INSTALL_RECOMMENDS:=1}"
: "${SSH_KEY_TYPE:=ed25519}"

: "${MODE:=full}"
: "${DRY_RUN:=0}"
: "${VERBOSE:=0}"
: "${LOW_END:=0}"
: "${NO_UPDATE:=0}"

: "${ENABLE_BASE:=1}"
: "${ENABLE_DEV:=1}"
: "${ENABLE_APPS:=1}"
: "${ENABLE_DOCKER:=1}"
: "${ENABLE_NODE:=1}"
: "${ENABLE_PYTHON:=1}"
: "${ENABLE_FLATPAK:=1}"
: "${ENABLE_TWEAKS:=1}"
: "${ENABLE_GIT_SSH:=1}"
: "${ENABLE_CLEANUP:=1}"

: "${MINT_POSTINSTALL_USER:=}"
: "${MINT_POSTINSTALL_HOME:=}"
: "${STATE_FILE:=}"
: "${TOTAL_RAM:=0}"
: "${APT_UPDATED:=0}"
: "${PENDING_REPO_REFRESH:=0}"
: "${EXPLICIT_FLATPAK:=0}"
